spring.application.name=fraud-service
server.port=8083

# ---------- KAFKA BOOTSTRAP ----------
spring.kafka.bootstrap-servers=localhost:9092

# ---------- CONSUMER (VERY IMPORTANT FIXES HERE) ----------
spring.kafka.consumer.group-id=fraud-group
spring.kafka.consumer.auto-offset-reset=earliest

# ? Use ErrorHandlingDeserializer (prevents crash on bad messages)
spring.kafka.consumer.key-deserializer=org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.ErrorHandlingDeserializer

# ? Delegate to actual serializers
spring.kafka.consumer.properties.spring.deserializer.key.delegate.class=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.properties.spring.deserializer.value.delegate.class=org.springframework.kafka.support.serializer.JsonDeserializer

# ? DO NOT use type headers (MUST MATCH Transaction-Service)
spring.kafka.consumer.properties.spring.json.use.type.headers=false

# ? Tell Kafka which class to deserialize INTO
spring.kafka.consumer.properties.spring.json.value.default.type=com.fintech.Fraud_Service.kafka.TransactionEvent

# ? Allow all packages
spring.kafka.consumer.properties.spring.json.trusted.packages=*

# ---------- PRODUCER (FIXED TYPO HERE ?) ----------
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
